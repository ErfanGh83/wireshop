export interface Address {
  id: string;
  province: string;
  city: string;
  postalCode: string;
  description: string;
  plaque: string;
}

export interface PostAddress {
  province: string;
  city: string;
  postalCode: string;
  description: string;
  plaque: string;
}